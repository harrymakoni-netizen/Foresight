'use client';
import {useState} from 'react';
import {Shield,ArrowRight,LockKeyhole} from 'lucide-react';
export default function SignIn(){const [key,setKey]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="signin"><section className="signin-brand"><Shield size={38}/><span>FORESIGHT</span><h1>WorkerSafe</h1><p>Every person registered.<br/>Every response recorded.</p><small>Software preview · simulator records only</small></section><section className="signin-form"><LockKeyhole size={27}/><h2>Your private workspace</h2><p>Enter your workspace access key to open WorkerSafe.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({key})});const d=await r.json();if(!r.ok)throw new Error(d.error);location.reload();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><label className="field">Access key<input type="password" required value={key} onChange={e=>setKey(e.target.value)} autoComplete="current-password"/></label>{error&&<p role="alert" className="form-error">{error}</p>}<button className="primary" disabled={busy}>{busy?'Opening workspace…':'Open WorkerSafe'}<ArrowRight size={17}/></button></form><small>The preview uses simulated mine data and an unvalidated risk register.</small></section></main>}
